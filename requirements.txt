# MeetingAssist 依赖
# 离线语音转写 (Qwen3-ASR + Whisper) 与翻译 (argos)
sherpa-onnx>=1.10.0
faster-whisper>=1.2
sounddevice>=0.4.6
numpy>=1.24
yt-dlp>=2024.1.1
argos-translate>=1.9
argostranslate
stanza

# 打包为 exe 时可选
# pyinstaller>=6.0
